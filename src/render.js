// import { initializeFramework } from './framework.js';
// import { Counter } from '../components/Counter.js';

// // Render function
// export function updateDOM(component) {
//   const root = document.getElementById('root');
//   root.innerHTML = component.render(); // Render the component
//   component.bind(); // Bind event listeners
// }

// // Initialize the framework with the updateDOM function
// initializeFramework(updateDOM);

// // Initial render
// document.addEventListener('DOMContentLoaded', () => {
//   const counterComponent = new Counter();
//   updateDOM(counterComponent);
// });